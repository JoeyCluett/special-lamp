#include <iostream>
#include <vector> // vector is more like Java arrays than regular c arrays

typedef vector<int>& ARRAY;

using namespace std;

void simpleBubbleSort(ARRAY arr);
void enhancedBubbleSort(ARRAY arr);
void insertionSort(ARRAY arr);
void selectionSort(ARRAY arr);

int main(int argc, char* argv[]) {

    return 0;
}

void simpleBubbleSort(ARRAY arr) {
    for(int i = 0; i < arr.size()-1; i++) {
        for(int j = 0; j < arr.size()-1; j++) {

        }
    }
}
