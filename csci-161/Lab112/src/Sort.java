/**
 *
 * @author joe
 */
public class Sort {
    // bubble sort is not required to run entire array
    public static void simpleBubbleSort(Object[] s, int arraySize) { 
        for(int i = 0; i < arraySize; i++) {
            for(int j = 0; j < (arraySize-1); j++) {
                
            }
        }
    }
}
